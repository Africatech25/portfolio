# Configuration pour Vercel (Copier ces valeurs dans le dashboard Vercel)

APP_NAME="Maurice CODJO Portfolio"
APP_ENV=production
APP_KEY=base64:+JdH/9RqCtoiaUyn1s8Ugmd3BCrtuuzbUKNMZBvpfmE=
APP_DEBUG=false
APP_URL=https://votre-app.vercel.app

# Logging pour Vercel
LOG_CHANNEL=stderr
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Cache et sessions adaptés pour serverless
CACHE_DRIVER=array
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=false

# Pas de base de données nécessaire (données statiques)
DB_CONNECTION=sqlite
DB_DATABASE=/tmp/database.sqlite

# Configuration Vercel spécifique
VIEW_COMPILED_PATH=/tmp/views
CACHE_PATH=/tmp/cache

# Mail (si nécessaire pour le formulaire contact)
MAIL_MAILER=log
MAIL_FROM_ADDRESS=contact@votre-domaine.com
MAIL_FROM_NAME="${APP_NAME}"

# Vite (pour les assets)
VITE_APP_NAME="${APP_NAME}"
